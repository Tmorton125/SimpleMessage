%div#site_navigation
  %h4= "Notification Center"
  %div
    %p= link_to "View Messages", main_app.messages_path 

  - $engine_manifests.each do |e|
    %h4= e[:navigation_title]
    %div
      - e[:links].each do |link|
        %p= link_to link[:title], eval(link[:url])

    
   